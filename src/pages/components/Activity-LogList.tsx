import styled from 'styled-components';

const Container = styled.div``;

interface IProps {}

const LogList: React.FunctionComponent<IProps> = () => {
  return <Container>활동기록 얼마 전(시간)</Container>;
};

export default LogList;
